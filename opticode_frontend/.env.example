# Local con `npm run dev`: déjalo vacío para que /api pase por el proxy de Vite → :8000 (sin CORS).
# Si el API está detrás de Docker (Nginx), usa el puerto publicado, p. ej. http://localhost:8080
# (no uses http://localhost sin puerto si Nginx no está en :80).
VITE_API_URL=
